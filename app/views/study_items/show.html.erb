<%= @study_item.full_title %>


<% if @study_item.done? %>
    Feito
<% else %>
    <%= button_to 'Marcar como feito', mark_as_done_study_item_path(@study_item), method: :patch%>
<% end %>

<%= link_to 'Editar', edit_study_item_path(@study_item) %>

<%= button_to "Deletar", @study_item, method: :delete %>